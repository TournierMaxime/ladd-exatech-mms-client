import { moderateScale } from '../../../utils/Responsive'

const header = {
  backgroundColor: '#fff',
  position: 'relative'
}
const title = {
  color: "#fff",
  fontSize: moderateScale(24),
  justifyContent: "flex-start"
}

const arrowIcon = {
    color: "#fff",
}

export default {
  header,
  title,
  arrowIcon
}
